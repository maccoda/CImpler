repo_url: git@gitlab.com/maccoda/cimpler.git
commit: master
user_build:
  before_build:
    - ls

  build:
    - touch test.txt
    - cat test.txt

  after_build:
    - echo Let's look at the build file
    - cat Cargo.toml
